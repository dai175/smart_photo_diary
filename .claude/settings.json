{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | grep '\\.dart$' | { read -r f; fvm dart format \"$f\"; } 2>/dev/null || true",
            "statusMessage": "Formatting Dart file..."
          }
        ]
      }
    ]
  }
}
